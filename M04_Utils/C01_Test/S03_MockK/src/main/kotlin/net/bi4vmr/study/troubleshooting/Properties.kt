package net.bi4vmr.study.troubleshooting

/**
 * 案例一：TODO 添加详情。
 *
 * @author bi4vmr@outlook.com
 * @since 1.0.0
 */
class Properties {

    // 定义名为 `state` 的属性。
    val state: Boolean = true

    // 定义名为 `getState` 的方法。
    fun getState(): Int {
        return 1
    }
}
